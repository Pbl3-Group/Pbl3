@{
    ViewData["Title"] = "Thiết Lập Hệ Thống";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="admin-main-header">
    <h1>@ViewData["Title"]</h1>
    <p class="lead">Cấu hình các tham số và tùy chọn chung cho toàn bộ hệ thống.</p>
</div>


@if (TempData["InfoMessage"] != null)
{
    <div class="alert alert-admin-info alert-dismissible fade show" role="alert">
        @TempData["InfoMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-admin-success alert-dismissible fade show" role="alert">
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-admin-danger alert-dismissible fade show" role="alert">
        @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="admin-card">
    <p>Nội dung thiết lập hệ thống sẽ được hiển thị ở đây.</p>
    <p>Ví dụ: cấu hình email, số lượng item trên mỗi trang, bật/tắt tính năng,...</p>
    
    @* 
    // Placeholder for a future form
    <form asp-action="ThietLapHeThong_Save" method="post">
        @Html.AntiForgeryToken()
        <div class="form-group mb-3">
            <label for="setting1" class="form-label fw-bold">Thiết lập ví dụ 1:</label>
            <input type="text" id="setting1" name="Setting1" class="form-control" value="Giá trị hiện tại" />
        </div>
        <button type="submit" class="btn btn-admin-primary"><i class="fas fa-save"></i> Lưu Thiết Lập</button>
    </form> 
    *@
</div>

<div class="mt-3">
    <a asp-action="Index" asp-controller="CauHinhHeThong" class="btn btn-admin-secondary"><i class="fas fa-arrow-left"></i> Quay lại Cấu hình chung</a>
</div>